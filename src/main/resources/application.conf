network {
  # Unique chain ID for EAGLCOIN
  chainId = "EAGL"
  magicBytes = [0xE1, 0xA9, 0x6C, 0x01]

  # Node info
  bindAddress = "0.0.0.0:9030"
  nodeName = "EAGL-Node"

  # Peers (none for now)
  knownPeers = []
}

blockchain {
  # Genesis block settings
  genesisHeight = 0
  initialDifficulty = 1
  blockInterval = 120s
  genesisTimestamp = 0
}
