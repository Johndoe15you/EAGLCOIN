network {
  # Unique chain ID for EAGLCOIN
  chainId = "EAGL"
  magicBytes = [0xE1, 0xA9, 0x6C, 0x01]

  # Local network port
  bindAddress = "0.0.0.0:9030"
  nodeName = "EAGL-Node"

  # Peers (none yet, since we’re starting fresh)
  knownPeers = []
}

blockchain {
  # Genesis block height
  genesisHeight = 0
  initialDifficulty = 1
  blockInterval = 120s
}
